---
- hosts: all
  tasks:
    - template:
        src: ./crtrdqm.sh 
        dest: /home/rdqmadmin/mqsc.sh
        owner: rdqmadmin
        group: mqm
        mode: u=rwx,g=rwx,o=rwx
    - command:
        cmd: "sh /home/rdqmadmin/mqsc.sh"
      become_user: rdqmadmin
    - service:
        name: firewalld
        state: started
        enabled: true
    - firewalld:
        port: "{{puerto}}/tcp"
        state: enabled
        immediate: true
        permanent: true
...
